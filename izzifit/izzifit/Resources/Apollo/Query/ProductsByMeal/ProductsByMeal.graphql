query ProductsByMeal($mealId: String!) {
    productsByMeal(mealId: $mealId) {
        Image {
            id
            urlIosFull
            urlIosPrev
        }
        ProductSources {
            amount
            name
        }
        amount
        id
        isToggled
        measure
        name
    }
}
